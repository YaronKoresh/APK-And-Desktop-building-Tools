import { Text, View, StyleSheet, ActivityIndicator, Animated } from 'react-native';
import { Avatar, Badge, withBadge, BottomSheet, Button, ButtonGroup, Card, ListItem, CheckBox, Chip, Divider, FAB, Header, Image, Input, LinearProgress, Overlay, PricingCard, Rating, AirbnbRating, SearchBar, Slider, SocialIcon, SpeedDial, Switch, Tab, Tile, Tooltip, Icon } from 'react-native-elements';
import CircularSlider from 'react-native-elements-universe';
import 'sqlite';
import '../functions/Giselle';
import '../data/style';


export default function Code() {
	return (
		<View style={css.out}>
			<View style={css.fence}>
				<View style={css.X5}>
					<Text>&zwnj;</Text>
				</View>
				<View style={css.X90}>
					<View style={css.Y5}>
						<Text>&zwnj;</Text>
					</View>
					<View style={css.Y90}>
						<View style={css.cell}>
							<View style={ltrc}>
								<Input style={css.strong} multiline='true' value={Giselle(`
															sql text select text from code where row_number() = 1 as one
	`													`)} onChangeText={Giselle(`
															sql text create table code(text) ; update code set text = ? where row_number() = 1 , [ this ] as one
	`													`)} />
							</View>
						</View>
					</View>
					<View style={css.Y5}>
						<Text>&zwnj;</Text>
					</View>
				</View>
				<View style={css.X5}>
					<Text>&zwnj;</Text>
				</View>
			</View>
		</View>
	)
}